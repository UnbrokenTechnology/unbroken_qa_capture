# Normalize all text files to LF everywhere (repo and working copy)
* text=auto eol=lf

# Force LF line endings for files that will run inside Linux containers
*.sh text eol=lf
*.py text eol=lf
Dockerfile text eol=lf
